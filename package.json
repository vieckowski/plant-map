{
  "name": "plant-map",
  "version": "1.0.0",
  "private": true,
  "description": "Mapa realizacji instalacji fotowoltaicznych (OpenStreetMap + Leaflet)",
  "scripts": {
    "dev": "npx --yes live-server --port=8000 --no-browser=false",
    "start": "python3 -m http.server 8000"
  }
}
